#!/usr/bin/python3

def print_list_integer(my_list=[]):
    """
    prints the integers in a list,
    assuming all list elements are integers
    """
    for inte in my_list:
        print("{}".format(inte))
